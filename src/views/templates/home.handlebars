<h1>Productos</h1>

{{#if mostrarProductos}}
{{#each productos}}
<div>
        <p>Titulo: {{this.title}}</p>
        <p>Precio: {{this.price}}</p>
        <img src="{{this.img}}" alt="imagen de {{this.title}}">

</div>
{{/each}}
{{else}}
<p>No se puede mostrar los productos</p>
{{/if}}